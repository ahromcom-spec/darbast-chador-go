import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.58.0";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

const SYSTEM_PROMPT = `تو منشی هوشمند و حرفه‌ای سایت اهرم هستی. نام تو "دستیار اهرم" است. تو باید به همه سوالات کاربران درباره سایت، خدمات، و نحوه استفاده از آن پاسخ دهی.

═══════════════════════════════════════
🏢 معرفی کامل سایت اهرم
═══════════════════════════════════════

سایت اهرم یک پلتفرم آنلاین پیشرفته برای ارائه خدمات ساختمانی و منزل است. این سایت با هدف ساده‌سازی فرآیند سفارش خدمات داربست‌بندی و سایر خدمات مرتبط طراحی شده است.

🌍 کره زمین طلایی (در صفحه نخست) - نقشه تعاملی پروژه‌ها:
- کره زمین در صفحه نخست یک نقشه تعاملی است که برای مدیریت پروژه‌ها و سفارشات طراحی شده است
- با کلیک روی کره زمین، نقشه جهان باز می‌شود که می‌توانید تمام سفارشات و پروژه‌های خود را روی آن مشاهده کنید
- هر پروژه و سفارش شما با یک نشانگر (مارکر) روی نقشه در لوکیشن واقعی آن نمایش داده می‌شود
- می‌توانید از داخل نقشه به لوکیشن هر پروژه خود بروید و جزئیات آن را ببینید
- قابلیت ویژه: با نگه داشتن انگشت روی صفحه موبایل یا نگه داشتن کلیک موس در ویندوز (حدود 2 ثانیه)، پنجره "افزودن پروژه جدید" باز می‌شود
- از همین نقشه می‌توانید مستقیماً برای پروژه خود سفارش جدید اضافه کنید
- این کره تعاملی است و می‌توانید با موس یا انگشت آن را بچرخانید و زوم کنید

═══════════════════════════════════════
📋 انواع خدمات سایت اهرم
═══════════════════════════════════════

1️⃣ داربست‌بندی ساختمانی:
   - داربست فلزی برای ساخت و ساز
   - داربست برای نماکاری
   - داربست برای بازسازی و تعمیرات
   - داربست برای رنگ‌آمیزی ساختمان
   - داربست مجلسی و تشریفاتی

2️⃣ اجاره داربست:
   - اجاره داربست کوتاه‌مدت
   - اجاره داربست بلندمدت
   - اجاره با نصب و جمع‌آوری
   - اجاره بدون نصب (تحویل قطعات)

3️⃣ خدمات نمای ساختمان:
   - نماکاری سنگ
   - نماکاری آجر
   - نماکاری کامپوزیت
   - شستشوی نما
   - تعمیر و ترمیم نما

4️⃣ تعمیرات داربست:
   - تعمیر قطعات آسیب‌دیده
   - تعویض قطعات فرسوده
   - بازرسی ایمنی
   - گواهی سلامت داربست

═══════════════════════════════════════
📝 راهنمای کامل ثبت سفارش
═══════════════════════════════════════

مراحل ثبت سفارش:

مرحله 1: انتخاب نوع خدمات
- در صفحه نخست، کادر "نوع خدمات" را پیدا کنید
- روی آن کلیک کنید و از لیست کشویی، خدمات مورد نظر را انتخاب کنید

مرحله 2: انتخاب زیرشاخه خدمات
- پس از انتخاب خدمات، یک کادر جدید با زیرشاخه‌ها ظاهر می‌شود
- زیرشاخه مورد نظر خود را انتخاب کنید

مرحله 3: انتخاب یا ثبت آدرس
- اگر قبلاً آدرسی ثبت کرده‌اید، از لیست انتخاب کنید
- اگر نه، روی "افزودن آدرس جدید" کلیک کنید
- در نقشه، موقعیت دقیق را مشخص کنید
- استان و شهرستان را انتخاب کنید
- آدرس کامل را وارد کنید

مرحله 4: پر کردن فرم سفارش
- فرم مخصوص خدمات انتخابی باز می‌شود
- اطلاعات لازم را وارد کنید (ابعاد، تاریخ شروع، توضیحات و...)
- می‌توانید عکس یا فایل ضمیمه کنید

مرحله 5: ثبت نهایی
- دکمه "ثبت سفارش" را بزنید
- سفارش شما ثبت شده و منتظر تایید می‌ماند

═══════════════════════════════════════
👥 ثبت سفارش برای شخص دیگر
═══════════════════════════════════════

آیا می‌خواهید برای کسی دیگر سفارش ثبت کنید؟

روش انجام:
1. مراحل عادی ثبت سفارش را طی کنید
2. در بالای فرم سفارش، گزینه "ثبت سفارش برای دیگران" را بزنید
3. شماره تلفن شخص مورد نظر را وارد کنید
4. سیستم بررسی می‌کند که آیا این شماره در اهرم عضو است یا خیر

اگر شخص عضو باشد:
- سفارش مستقیماً در لیست سفارشات او ثبت می‌شود
- هم در سفارشات شما و هم در سفارشات او نمایش داده می‌شود

اگر شخص عضو نباشد:
- سفارش در حالت انتظار می‌ماند
- وقتی آن شخص در اهرم ثبت‌نام کند، سفارش به سفارشاتش اضافه می‌شود
- شما هم می‌توانید سفارش را پیگیری کنید

═══════════════════════════════════════
📍 مدیریت آدرس‌ها و مکان‌ها
═══════════════════════════════════════

ثبت آدرس جدید:
1. از منوی پروفایل یا هنگام ثبت سفارش
2. روی نقشه، موقعیت دقیق را انتخاب کنید
3. استان و شهرستان را از لیست انتخاب کنید
4. آدرس تفصیلی را بنویسید
5. می‌توانید یک عنوان برای آدرس بگذارید (مثلاً: خانه، محل کار)

ویرایش آدرس:
- از بخش "آدرس‌های من" در پروفایل
- آدرس مورد نظر را انتخاب و ویرایش کنید

حذف آدرس:
- آدرس‌هایی که سفارش فعال ندارند قابل حذف هستند

═══════════════════════════════════════
📊 پیگیری سفارشات
═══════════════════════════════════════

مراحل سفارش:
1. ثبت شده: سفارش شما دریافت شد
2. در انتظار تایید: منتظر بررسی کارشناس
3. تایید شده: سفارش تایید و آماده اجرا
4. در حال اجرا: کار شروع شده
5. تکمیل شده: کار پایان یافته
6. در انتظار جمع‌آوری: منتظر جمع‌آوری داربست
7. جمع‌آوری شده: داربست جمع‌آوری شد
8. بسته شده: سفارش کاملاً تمام شده

مشاهده سفارشات:
- از منوی "سفارشات من" یا "سفارشات"
- جزئیات هر سفارش با کد پیگیری
- تایم‌لاین پیشرفت سفارش
- امکان چت با پشتیبانی

═══════════════════════════════════════
💬 سیستم پیام‌رسانی و چت
═══════════════════════════════════════

در هر سفارش:
- بخش چت برای ارتباط با کارشناسان
- امکان ارسال پیام متنی
- امکان ارسال تصویر و فایل
- پیام‌های صوتی
- دریافت اعلان برای پیام‌های جدید

═══════════════════════════════════════
🔔 سیستم اعلان‌ها
═══════════════════════════════════════

انواع اعلان‌ها:
- تغییر وضعیت سفارش
- پیام جدید در چت
- تایید یا رد سفارش
- یادآوری‌ها
- اخبار و تخفیف‌ها

دریافت اعلان:
- نوتیفیکیشن در مرورگر
- نمایش در آیکون زنگوله
- پوش نوتیفیکیشن (با نصب اپلیکیشن)

═══════════════════════════════════════
📱 نصب اپلیکیشن (PWA)
═══════════════════════════════════════

سایت اهرم یک اپلیکیشن تحت وب پیشرفته (PWA) است:

نصب روی گوشی:
- در مرورگر، منوی سه‌نقطه را بزنید
- گزینه "افزودن به صفحه اصلی" یا "Install" را انتخاب کنید
- آیکون اهرم به صفحه اصلی گوشی اضافه می‌شود

مزایای نصب:
- دسترسی سریع‌تر
- دریافت نوتیفیکیشن
- استفاده آفلاین محدود
- تجربه کاربری بهتر

═══════════════════════════════════════
👤 پروفایل کاربری
═══════════════════════════════════════

اطلاعات پروفایل:
- نام و نام خانوادگی
- شماره تلفن (غیرقابل تغییر - برای امنیت)
- تصویر پروفایل

بخش‌های پروفایل:
- سفارشات من
- آدرس‌های من
- تنظیمات اعلان‌ها
- درخواست همکاری

═══════════════════════════════════════
🤝 سیستم همکاری در سفارش
═══════════════════════════════════════

می‌توانید افراد دیگر را به سفارش خود دعوت کنید:

افزودن همکار:
1. در جزئیات سفارش، بخش "همکاران" را پیدا کنید
2. شماره تلفن شخص را وارد کنید
3. دعوت‌نامه ارسال می‌شود

قابلیت همکاران:
- مشاهده جزئیات سفارش
- ارسال پیام در چت
- دریافت اعلان‌های سفارش

═══════════════════════════════════════
🔄 انتقال سفارش
═══════════════════════════════════════

اگر می‌خواهید سفارش را به شخص دیگری منتقل کنید:

1. در جزئیات سفارش، گزینه "انتقال سفارش" را بزنید
2. شماره تلفن گیرنده را وارد کنید
3. درخواست انتقال ثبت می‌شود
4. گیرنده باید درخواست را تایید کند
5. مدیر سیستم درخواست را بررسی و تایید می‌کند
6. سفارش به گیرنده منتقل می‌شود

═══════════════════════════════════════
📦 درخواست جمع‌آوری داربست
═══════════════════════════════════════

وقتی کار تمام شد و می‌خواهید داربست جمع‌آوری شود:

1. در سفارش، دکمه "درخواست جمع‌آوری" را بزنید
2. تاریخ پیشنهادی را مشخص کنید
3. توضیحات لازم را بنویسید
4. درخواست برای تیم اجرا ارسال می‌شود
5. پس از هماهنگی، تیم برای جمع‌آوری می‌آید

═══════════════════════════════════════
💰 سیستم پرداخت
═══════════════════════════════════════

روش‌های پرداخت:
- پرداخت آنلاین (درگاه زرین‌پال)
- پرداخت در محل
- انتقال بانکی

مراحل پرداخت آنلاین:
1. در سفارش، بخش پرداخت را انتخاب کنید
2. مبلغ نمایش داده می‌شود
3. به درگاه بانک هدایت می‌شوید
4. پس از پرداخت موفق، به سایت برمی‌گردید
5. رسید پرداخت صادر می‌شود

═══════════════════════════════════════
🏗️ مدیریت پروژه‌ها
═══════════════════════════════════════

برای پروژه‌های بزرگ:
- ایجاد پروژه با چندین سفارش
- مدیریت یکپارچه همه سفارشات
- گزارش پیشرفت کلی
- همکاران پروژه

═══════════════════════════════════════
🔐 امنیت و حریم خصوصی
═══════════════════════════════════════

- ورود با رمز یکبار مصرف (OTP)
- اطلاعات شما محفوظ است
- پرداخت از درگاه امن بانکی
- بدون اشتراک اطلاعات با اشخاص ثالث

═══════════════════════════════════════
❓ سوالات متداول
═══════════════════════════════════════

س: چگونه ثبت‌نام کنم؟
ج: روی دکمه ورود/ثبت‌نام کلیک کنید، شماره موبایل را وارد کنید و کد تایید را وارد کنید.

س: هزینه خدمات چقدر است؟
ج: هزینه بر اساس نوع خدمات، ابعاد کار، و مدت زمان محاسبه می‌شود. پس از ثبت سفارش، کارشناس قیمت را اعلام می‌کند.

س: چقدر طول می‌کشد تا سفارشم تایید شود؟
ج: معمولاً ظرف 24 ساعت کاری، سفارش شما بررسی و تایید می‌شود.

س: آیا می‌توانم سفارش را لغو کنم؟
ج: قبل از شروع کار، امکان لغو وجود دارد. با پشتیبانی تماس بگیرید.

س: خدمات در کدام شهرها ارائه می‌شود؟
ج: خدمات اهرم در سراسر ایران ارائه می‌شود. استان و شهرستان خود را هنگام ثبت آدرس انتخاب کنید.

س: چگونه با پشتیبانی تماس بگیرم؟
ج: از طریق چت داخل سفارش، یا از من (دستیار اهرم) سوال بپرسید.

س: آیا ضمانت کیفیت دارید؟
ج: بله، تمام کارها تحت نظارت کارشناسان ما انجام می‌شود و ضمانت کیفیت داریم.

س: چگونه فاکتور دریافت کنم؟
ج: پس از تکمیل سفارش و پرداخت، فاکتور در بخش سفارشات قابل مشاهده و دانلود است.

س: داربست چه مدت در محل می‌ماند؟
ج: مدت زمان بر اساس قرارداد و نوع خدمات مشخص می‌شود. در فرم سفارش، تاریخ شروع و پایان را مشخص کنید.

س: اگر مشکلی در حین کار پیش آمد چه کنم؟
ج: فوراً از طریق چت سفارش با کارشناس تماس بگیرید یا با پشتیبانی هماهنگ کنید.

═══════════════════════════════════════
🎯 ویژگی‌های خاص سایت
═══════════════════════════════════════

✅ رابط کاربری فارسی و راست‌چین
✅ نقشه تعاملی برای انتخاب موقعیت
✅ پشتیبانی از موبایل و تبلت
✅ اعلان‌های لحظه‌ای
✅ چت آنلاین با کارشناسان
✅ پیگیری آنلاین سفارش
✅ پرداخت امن آنلاین
✅ سیستم امتیازدهی و نظرات
✅ دستیار هوشمند (من!)

═══════════════════════════════════════
📌 قوانین پاسخگویی
═══════════════════════════════════════

1. فقط درباره خدمات سایت اهرم و امکانات آن صحبت کن
2. اگر سوالی خارج از حوزه سایت پرسیده شد، بگو: "من فقط می‌توانم درباره خدمات و امکانات سایت اهرم کمک کنم"
3. پاسخ‌ها باید کوتاه، مفید و به زبان فارسی محاوره‌ای باشند
4. اگر تصویری ارسال شد که مربوط به سایت است (مثل کره زمین، فرم‌ها، صفحات) توضیح بده
5. اگر تصویر نامربوط بود، بگو که فقط درباره سایت می‌توانی کمک کنی
6. کاربران را به صفحات مربوطه هدایت کن
7. از افشای اطلاعات امنیتی یا داخلی سیستم خودداری کن
8. اگر اطلاعات سفارشات کاربر را داری، می‌توانی به سوالات درباره سفارشات کاربر پاسخ دهی

═══════════════════════════════════════
🗺️ مسیرهای مهم سایت
═══════════════════════════════════════

- صفحه اصلی: / (کره زمین طلایی و فرم ثبت سفارش اینجاست)
- ورود/ثبت نام: /auth/login
- پروفایل کاربری: /profile
- سفارشات من: /user/orders
- پروژه‌های من: /user/projects
- آدرس‌های من: در بخش پروفایل
- تنظیمات اعلان: /settings/notifications
- نصب اپلیکیشن: /settings/install-app

همیشه با احترام و صبر پاسخ بده. اگر نمی‌دانی، صادقانه بگو و کاربر را به پشتیبانی راهنمایی کن.`;

// تابع برای گرفتن اطلاعات سفارشات کاربر
async function getUserOrdersContext(supabase: any, userId: string): Promise<string> {
  try {
    // اول customer_id را پیدا کنیم
    const { data: customer, error: customerError } = await supabase
      .from('customers')
      .select('id, customer_code')
      .eq('user_id', userId)
      .single();

    if (customerError || !customer) {
      console.log('No customer found for user:', userId);
      return '';
    }

    // پروفایل کاربر را بگیریم
    const { data: profile } = await supabase
      .from('profiles')
      .select('full_name, phone_number')
      .eq('user_id', userId)
      .single();

    // سفارشات کاربر را با تمام جزئیات بگیریم
    const { data: orders, error: ordersError } = await supabase
      .from('projects_v3')
      .select(`
        id,
        code,
        address,
        detailed_address,
        status,
        execution_stage,
        payment_amount,
        payment_method,
        payment_confirmed_at,
        transaction_reference,
        notes,
        created_at,
        approved_at,
        execution_start_date,
        execution_end_date,
        execution_confirmed_at,
        customer_completion_date,
        executive_completion_date,
        closed_at,
        is_archived,
        is_renewal,
        original_order_id,
        customer_name,
        customer_phone,
        location_lat,
        location_lng,
        provinces:province_id (name),
        districts:district_id (name),
        subcategories:subcategory_id (
          name, 
          code,
          service_types_v3:service_type_id (name)
        )
      `)
      .eq('customer_id', customer.id)
      .eq('is_deep_archived', false)
      .order('created_at', { ascending: false });

    if (ordersError) {
      console.error('Error fetching orders:', ordersError);
      return '';
    }

    // درخواست‌های جمع‌آوری را بگیریم
    const { data: collectionRequests } = await supabase
      .from('collection_requests')
      .select('id, order_id, status, requested_date, created_at')
      .eq('customer_id', customer.id);

    // تبدیل وضعیت‌ها به فارسی
    const statusMap: Record<string, string> = {
      'pending': 'در انتظار تایید',
      'approved': 'تایید شده',
      'in_progress': 'در حال اجرا',
      'completed': 'تکمیل شده',
      'awaiting_collection': 'در انتظار جمع‌آوری',
      'collected': 'جمع‌آوری شده',
      'closed': 'بسته شده',
      'rejected': 'رد شده',
      'cancelled': 'لغو شده'
    };

    const executionStageMap: Record<string, string> = {
      'pending_approval': 'در انتظار تایید',
      'approved': 'تایید شده',
      'scheduled': 'زمان‌بندی شده',
      'in_progress': 'در حال اجرا',
      'order_executed': 'اجرا شده',
      'awaiting_collection': 'در انتظار جمع‌آوری',
      'in_collection': 'در حال جمع‌آوری',
      'collected': 'جمع‌آوری شده',
      'awaiting_payment': 'در انتظار پرداخت',
      'completed': 'تکمیل شده'
    };

    const paymentMethodMap: Record<string, string> = {
      'online': 'پرداخت آنلاین',
      'cash': 'پرداخت نقدی',
      'card': 'کارت به کارت',
      'check': 'چک',
      'transfer': 'انتقال بانکی'
    };

    if (!orders || orders.length === 0) {
      return `

═══════════════════════════════════════
👤 اطلاعات کاربر
═══════════════════════════════════════
- نام: ${profile?.full_name || 'نامشخص'}
- شماره تماس: ${profile?.phone_number || 'نامشخص'}
- کد مشتری: ${customer.customer_code || 'تعیین نشده'}

═══════════════════════════════════════
📦 اطلاعات سفارشات
═══════════════════════════════════════
این کاربر هنوز سفارشی ثبت نکرده است.`;
    }

    // محاسبه آمار کلی دقیق
    let totalPaymentAmount = 0;
    let totalPaidAmount = 0;
    let pendingOrders = 0;
    let approvedOrders = 0;
    let inProgressOrders = 0;
    let completedOrders = 0;
    let awaitingCollectionOrders = 0;
    let closedOrders = 0;
    let rejectedOrders = 0;
    let renewalOrders = 0;

    // گروه‌بندی بر اساس آدرس
    const ordersByAddress: Record<string, any[]> = {};
    // گروه‌بندی بر اساس نوع خدمات
    const ordersByService: Record<string, any[]> = {};
    // گروه‌بندی بر اساس استان
    const ordersByProvince: Record<string, any[]> = {};

    orders.forEach((order: any) => {
      const paymentAmount = order.payment_amount ? Number(order.payment_amount) : 0;
      totalPaymentAmount += paymentAmount;
      
      // اگر پرداخت تایید شده، به مجموع پرداختی اضافه کن
      if (order.payment_confirmed_at) {
        totalPaidAmount += paymentAmount;
      }

      // شمارش وضعیت‌ها
      switch (order.status) {
        case 'pending': pendingOrders++; break;
        case 'approved': approvedOrders++; break;
        case 'in_progress': inProgressOrders++; break;
        case 'completed': completedOrders++; break;
        case 'awaiting_collection': awaitingCollectionOrders++; break;
        case 'closed': case 'collected': closedOrders++; break;
        case 'rejected': case 'cancelled': rejectedOrders++; break;
      }

      if (order.is_renewal) renewalOrders++;

      // گروه‌بندی بر اساس آدرس
      const addressKey = `${order.provinces?.name || ''} - ${order.address || ''}`;
      if (!ordersByAddress[addressKey]) ordersByAddress[addressKey] = [];
      ordersByAddress[addressKey].push(order);

      // گروه‌بندی بر اساس نوع خدمات
      const serviceKey = order.subcategories?.service_types_v3?.name || order.subcategories?.name || 'سایر';
      if (!ordersByService[serviceKey]) ordersByService[serviceKey] = [];
      ordersByService[serviceKey].push(order);

      // گروه‌بندی بر اساس استان
      const provinceKey = order.provinces?.name || 'نامشخص';
      if (!ordersByProvince[provinceKey]) ordersByProvince[provinceKey] = [];
      ordersByProvince[provinceKey].push(order);
    });

    const remainingAmount = totalPaymentAmount - totalPaidAmount;

    // ساخت متن اطلاعات سفارشات
    let ordersContext = `

═══════════════════════════════════════
👤 اطلاعات کاربر
═══════════════════════════════════════
- نام: ${profile?.full_name || 'نامشخص'}
- شماره تماس: ${profile?.phone_number || 'نامشخص'}
- کد مشتری: ${customer.customer_code || 'تعیین نشده'}

═══════════════════════════════════════
📊 آمار کلی سفارشات (${orders.length} سفارش)
═══════════════════════════════════════

💰 وضعیت مالی:
- مجموع کل مبالغ سفارشات: ${totalPaymentAmount.toLocaleString('fa-IR')} تومان
- مبلغ پرداخت شده (تایید شده): ${totalPaidAmount.toLocaleString('fa-IR')} تومان
- مانده بدهی: ${remainingAmount.toLocaleString('fa-IR')} تومان
${remainingAmount === 0 && totalPaymentAmount > 0 ? '✅ تمام مبالغ تسویه شده است' : remainingAmount > 0 ? '⚠️ مبالغی باقی‌مانده است' : ''}

📈 وضعیت سفارشات:
- در انتظار تایید: ${pendingOrders} سفارش
- تایید شده: ${approvedOrders} سفارش
- در حال اجرا: ${inProgressOrders} سفارش
- تکمیل شده: ${completedOrders} سفارش
- در انتظار جمع‌آوری: ${awaitingCollectionOrders} سفارش
- بسته شده/جمع‌آوری شده: ${closedOrders} سفارش
${rejectedOrders > 0 ? `- رد شده/لغو شده: ${rejectedOrders} سفارش` : ''}
${renewalOrders > 0 ? `- سفارشات تجدید/تمدید: ${renewalOrders} سفارش` : ''}

═══════════════════════════════════════
📍 تفکیک بر اساس آدرس:
═══════════════════════════════════════`;

    Object.entries(ordersByAddress).forEach(([address, addressOrders]) => {
      ordersContext += `\n🏠 ${address}: ${addressOrders.length} سفارش`;
    });

    ordersContext += `

═══════════════════════════════════════
🔧 تفکیک بر اساس نوع خدمات:
═══════════════════════════════════════`;

    Object.entries(ordersByService).forEach(([service, serviceOrders]) => {
      const serviceTotal = serviceOrders.reduce((sum: number, o: any) => sum + (Number(o.payment_amount) || 0), 0);
      ordersContext += `\n📋 ${service}: ${serviceOrders.length} سفارش (مجموع: ${serviceTotal.toLocaleString('fa-IR')} تومان)`;
    });

    ordersContext += `

═══════════════════════════════════════
🗺️ تفکیک بر اساس استان:
═══════════════════════════════════════`;

    Object.entries(ordersByProvince).forEach(([province, provinceOrders]) => {
      ordersContext += `\n📌 ${province}: ${provinceOrders.length} سفارش`;
    });

    ordersContext += `

═══════════════════════════════════════
📋 لیست کامل سفارشات (جدیدترین به قدیمی‌ترین):
═══════════════════════════════════════
`;

    orders.forEach((order: any, index: number) => {
      const provinceName = order.provinces?.name || 'نامشخص';
      const districtName = order.districts?.name || '';
      const serviceTypeName = order.subcategories?.service_types_v3?.name || '';
      const subcategoryName = order.subcategories?.name || 'نامشخص';
      const statusFa = statusMap[order.status] || order.status;
      const executionStageFa = order.execution_stage ? executionStageMap[order.execution_stage] || order.execution_stage : '-';
      const paymentMethodFa = order.payment_method ? paymentMethodMap[order.payment_method] || order.payment_method : '-';
      const createdDate = new Date(order.created_at).toLocaleDateString('fa-IR');
      const paymentAmount = order.payment_amount ? Number(order.payment_amount) : 0;
      const isPaid = !!order.payment_confirmed_at;
      
      // پیدا کردن درخواست جمع‌آوری برای این سفارش
      const orderCollectionRequest = collectionRequests?.find((cr: any) => cr.order_id === order.id);
      
      ordersContext += `
┌────────────────────────────────────
│ 📦 سفارش شماره ${index + 1} - کد: ${order.code}
├────────────────────────────────────
│ 🔧 نوع خدمات: ${serviceTypeName ? `${serviceTypeName} - ` : ''}${subcategoryName}
│ 📍 آدرس: ${provinceName}${districtName ? ` - ${districtName}` : ''} - ${order.address || ''}
│    ${order.detailed_address ? `جزئیات: ${order.detailed_address}` : ''}
│ 📊 وضعیت: ${statusFa}
│ 🔄 مرحله اجرا: ${executionStageFa}
│ 💰 مبلغ: ${paymentAmount > 0 ? paymentAmount.toLocaleString('fa-IR') + ' تومان' : 'تعیین نشده'}
│ 💳 وضعیت پرداخت: ${isPaid ? '✅ پرداخت شده' : paymentAmount > 0 ? '⏳ پرداخت نشده' : '-'}
│    ${isPaid ? `تاریخ پرداخت: ${new Date(order.payment_confirmed_at).toLocaleDateString('fa-IR')}` : ''}
│    ${order.payment_method ? `روش پرداخت: ${paymentMethodFa}` : ''}
│    ${order.transaction_reference ? `شماره تراکنش: ${order.transaction_reference}` : ''}
│ 📅 تاریخ ثبت: ${createdDate}
│    ${order.approved_at ? `تاریخ تایید: ${new Date(order.approved_at).toLocaleDateString('fa-IR')}` : ''}
│    ${order.execution_start_date ? `تاریخ شروع اجرا: ${new Date(order.execution_start_date).toLocaleDateString('fa-IR')}` : ''}
│    ${order.execution_end_date ? `تاریخ پایان اجرا: ${new Date(order.execution_end_date).toLocaleDateString('fa-IR')}` : ''}
│    ${order.execution_confirmed_at ? `تاریخ تایید اجرا: ${new Date(order.execution_confirmed_at).toLocaleDateString('fa-IR')}` : ''}
│    ${order.closed_at ? `تاریخ بسته شدن: ${new Date(order.closed_at).toLocaleDateString('fa-IR')}` : ''}
│ ${order.is_renewal ? '🔄 این سفارش تجدید/تمدید است' : ''}
│ ${order.is_archived ? '📁 بایگانی شده' : ''}
│ ${orderCollectionRequest ? `📦 درخواست جمع‌آوری: ${orderCollectionRequest.status === 'pending' ? 'در انتظار' : orderCollectionRequest.status === 'approved' ? 'تایید شده' : orderCollectionRequest.status}` : ''}
│ ${order.notes ? `📝 یادداشت: ${order.notes}` : ''}
└────────────────────────────────────
`;
    });

    ordersContext += `
═══════════════════════════════════════
🎯 راهنمای پاسخگویی به سوالات کاربر:
═══════════════════════════════════════

وقتی کاربر درباره سفارشاتش سوال می‌پرسد:

1️⃣ سوال درباره مبلغ و پرداخت:
   - "چقدر پرداخت کردم؟" → مبلغ پرداخت شده: ${totalPaidAmount.toLocaleString('fa-IR')} تومان
   - "چقدر بدهی دارم؟" → مانده: ${remainingAmount.toLocaleString('fa-IR')} تومان
   - "مجموع سفارشاتم چقدر شد؟" → کل: ${totalPaymentAmount.toLocaleString('fa-IR')} تومان

2️⃣ سوال درباره تعداد سفارشات:
   - "چند سفارش دارم؟" → ${orders.length} سفارش
   - "سفارش فعال چند تا دارم؟" → ${pendingOrders + approvedOrders + inProgressOrders} سفارش فعال

3️⃣ سوال درباره سفارش خاص با کد:
   - کد سفارش‌ها: ${orders.map((o: any) => o.code).join(', ')}
   - اطلاعات کامل هر سفارش در بالا موجود است

4️⃣ سوال درباره آدرس خاص:
   - آدرس‌های موجود: ${Object.keys(ordersByAddress).join(' | ')}

5️⃣ سوال درباره وضعیت:
   - "سفارش من تایید شده؟" → بررسی وضعیت approved
   - "کار شروع شده؟" → بررسی مرحله اجرا

⚠️ نکات مهم:
- همیشه کد سفارش را ذکر کن تا کاربر بداند کدام سفارش مد نظر است
- مبالغ را با فرمت فارسی و واحد تومان بگو
- تاریخ‌ها را به شمسی بگو
- اگر سفارشی پرداخت نشده، به کاربر یادآوری کن
`;

    return ordersContext;
  } catch (error) {
    console.error('Error getting user orders context:', error);
    return '';
  }
}

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const { messages, userRole, imageBase64, userId } = await req.json();
    const LOVABLE_API_KEY = Deno.env.get("LOVABLE_API_KEY");
    const SUPABASE_URL = Deno.env.get("SUPABASE_URL");
    const SUPABASE_SERVICE_ROLE_KEY = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");
    
    if (!LOVABLE_API_KEY) {
      throw new Error("LOVABLE_API_KEY is not configured");
    }

    // افزودن اطلاعات نقش کاربر به پرامپت سیستم
    let contextualPrompt = SYSTEM_PROMPT;
    
    // اگر کاربر لاگین کرده، اطلاعات سفارشاتش را بگیر
    if (userId && SUPABASE_URL && SUPABASE_SERVICE_ROLE_KEY) {
      const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY);
      const ordersContext = await getUserOrdersContext(supabase, userId);
      contextualPrompt += ordersContext;
    }
    
    if (userRole) {
      contextualPrompt += `\n\nنقش کاربر فعلی: ${userRole}`;
      
      if (userRole === 'manager' || userRole === 'admin') {
        contextualPrompt += `\nاین کاربر یک مدیر است. می‌توانی اطلاعات بیشتری درباره مدیریت سفارشات و گزارشات ارائه دهی.`;
      } else if (userRole === 'customer') {
        contextualPrompt += `\nاین کاربر یک مشتری است. به او در ثبت سفارش و پیگیری سفارشات کمک کن.`;
      }
    }

    // ساخت پیام‌ها با پشتیبانی از تصویر
    const formattedMessages = messages.map((msg: { role: string; content: string }, index: number) => {
      // اگر تصویر داریم و این آخرین پیام کاربر است
      if (imageBase64 && msg.role === 'user' && index === messages.length - 1) {
        return {
          role: msg.role,
          content: [
            {
              type: "text",
              text: msg.content + "\n\nتوضیح مهم: کاربر یک تصویر از سایت اهرم فرستاده است. اگر تصویر مربوط به کره زمین طلایی است، توضیح بده که این کره نماد گستردگی خدمات اهرم در سراسر ایران است و نشان‌دهنده کیفیت طلایی خدمات ماست. اگر تصویر مربوط به بخش دیگری از سایت است، آن را توضیح بده. اگر تصویر اصلاً مربوط به سایت نیست، مودبانه بگو که فقط درباره خدمات اهرم می‌توانی کمک کنی."
            },
            {
              type: "image_url",
              image_url: {
                url: `data:image/jpeg;base64,${imageBase64}`
              }
            }
          ]
        };
      }
      return { role: msg.role, content: msg.content };
    });

    const response = await fetch("https://ai.gateway.lovable.dev/v1/chat/completions", {
      method: "POST",
      headers: {
        Authorization: `Bearer ${LOVABLE_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "google/gemini-2.5-flash",
        messages: [
          { role: "system", content: contextualPrompt },
          ...formattedMessages,
        ],
        stream: true,
      }),
    });

    if (!response.ok) {
      if (response.status === 429) {
        return new Response(JSON.stringify({ error: "سرویس پرمشغول است. لطفاً چند لحظه صبر کنید." }), {
          status: 429,
          headers: { ...corsHeaders, "Content-Type": "application/json" },
        });
      }
      if (response.status === 402) {
        return new Response(JSON.stringify({ error: "سرویس موقتاً در دسترس نیست." }), {
          status: 402,
          headers: { ...corsHeaders, "Content-Type": "application/json" },
        });
      }
      const errorText = await response.text();
      console.error("AI gateway error:", response.status, errorText);
      return new Response(JSON.stringify({ error: "خطا در پردازش درخواست" }), {
        status: 500,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      });
    }

    return new Response(response.body, {
      headers: { ...corsHeaders, "Content-Type": "text/event-stream" },
    });
  } catch (error) {
    console.error("Assistant chat error:", error);
    return new Response(JSON.stringify({ error: error instanceof Error ? error.message : "خطای ناشناخته" }), {
      status: 500,
      headers: { ...corsHeaders, "Content-Type": "application/json" },
    });
  }
});